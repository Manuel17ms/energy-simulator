#include <iostream>

using namespace std;

double taylor(int a,int b,int c){
    if(k==n<1){
        return result;
    }else{
        int result=for(int i=0)
    }
}

int main(){
    int k=0,n=0,x=0;
    cout<<"Inserisci due interi"<<endl;
    cin>>x>>n;
    cout<<"Il valore approssimato del seno di "<<x<<" e' "<<taylor(k,n,x)<<endl;
}